{
  "name": "@markcards/client",
  "type": "module",
  "scripts": {
    "build": "bun run build.mjs",
    "dev": "bun run build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "bun-types": "latest",
    "esbuild": "^0.20.0",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.3.3",
    "vitest": "^3.0.0"
  }
}
